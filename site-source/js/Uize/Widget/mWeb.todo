This is a TO DO document for the =Uize.Widget.mWeb= module.

Proposed Methods
	selectNodes
		Selects nodes based on CSS selection syntax within the root node, returning a =Uize.Web= object.
		
		NOTES
		- selectNodes(selector)
		- CSS classes could be auto-prefixed using the widget classes CSS class prefix
	
